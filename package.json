{
  "name": "scc-accountability",
  "version": "1.0.0",
  "description": "St. Clair County Sheriff Accountability Project",
  "scripts": {
    "dev": "npm run dev:index && concurrently \"npm run tailwind:watch\" \"hugo server --buildDrafts\"",
    "dev:index": "npm run tailwind:build && hugo --gc && npm run pagefind:dev",
    "tailwind:watch": "npx @tailwindcss/cli -i ./tailwind.css -o ./assets/css/main.css --watch",
    "tailwind:build": "npx @tailwindcss/cli -i ./tailwind.css -o ./assets/css/main.css --minify",
    "build": "npm run tailwind:build && hugo --minify && npm run pagefind",
    "pagefind": "npx pagefind --site public --output-path public/pagefind",
    "pagefind:dev": "npx pagefind --site public --output-path static/pagefind",
    "preview": "hugo server --buildDrafts"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "concurrently": "^9.0.0",
    "pagefind": "^1.3.0"
  }
}
